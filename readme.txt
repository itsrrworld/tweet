** First go through requirements.txt **

* Note: This app was created under Virtual Environment * 

-------------------------------------------------------------------

Open the src folder and open command prompt(or any CLI) there.

To Run the app => python manage.py runserver (Make sure that python is installed).

Open the url shown in the CLI in any browser(with javascript enabled).

Then, in the browser Go To => Menu => Register & Register yourself.

Then, Login Yourself & use the app.

-------------------------------------------------------------------

** ADMIN DETAILS **

(url/admin)(127.0.0.1:8000/admin)
username : itsrr
password: brockunder1997


-------------------------------------------------------------------


** TO RUN TEST **

type command => python manage.py test


--------------------------------------------------------------------


** REST FRAMEWORK **

To view REST API,

GO TO => locahosturl/api/tweet (example: 127.0.0.1:8000/api/tweet)


*------------------------------ * * ---------------------------------*